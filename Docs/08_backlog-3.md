# Журнал задач. Этап 3. Серверное ядро первой рабочей версии

Связанный этап:
Этап 3 дорожной карты VBank.

Состояние:
Предстоит.

Назначение журнала задач:
Определить крупные области работ по созданию первой рабочей версии серверной части VBank на Python.

Граница журнала задач:
Документ не детализирует реализацию, не описывает пошаговое выполнение задач и не заменяет планы задач. Подробная детализация будет добавляться по мере перехода к этапу.

В границу этапа не входят консольный клиент и веб-приложение. Они используют серверное API на следующих этапах.

Связь с плановым контуром:
Перед началом этапа крупные области работ должны быть разложены на отдельные планы задач.

---

## 1. Подготовка Python-проекта

Состояние:
Предстоит.

Смысл задачи:
Подготовить структуру серверной части, зависимости, конфигурацию, запуск приложения и базовую связку с PostgreSQL.

Ожидаемый результат:
Серверная часть запускается локально, имеет единый способ настройки и готова к добавлению прикладных модулей.

---

## 2. Каркас API и общие технические правила

Состояние:
Предстоит.

Смысл задачи:
Реализовать базовый путь `/api/v1`, единый формат ошибок, `request_id`, структурированные логи и общие зависимости FastAPI.

Ожидаемый результат:
Все будущие маршруты API подключаются к единому каркасу и возвращают согласованный результат.

---

## 3. Модель хранения и миграции

Состояние:
Предстоит.

Смысл задачи:
Реализовать модели SQLAlchemy, миграции Alembic и начальные справочники в соответствии с моделью данных.

Ожидаемый результат:
PostgreSQL создаётся воспроизводимо, ограничения данных проверяются на уровне БД и серверной части.

---

## 4. Регистрация, вход и сессии

Состояние:
Предстоит.

Смысл задачи:
Реализовать регистрацию, вход, выдачу `access token`, отзывной `refresh token`, обновление и завершение сессии.

Ожидаемый результат:
Пользователь может войти в систему, а серверная часть может определить его роль, статус и активную сессию.

---

## 5. Пользователи, роли и статусы

Состояние:
Предстоит.

Смысл задачи:
Реализовать управление пользователями, ролями, блокировками и проверками доступа по ролям `Client`, `Operator`, `Admin`.

Ожидаемый результат:
Клиент, оператор и администратор получают разные права в соответствии с требованиями VBank.

---

## 6. Счета

Состояние:
Предстоит.

Смысл задачи:
Реализовать открытие, просмотр, состояние и закрытие счетов.

Ожидаемый результат:
Счета создаются и изменяются по правилам VBank, включая ограничения по валюте, типу счёта и состоянию.

---

## 7. Финансовые операции

Состояние:
Предстоит.

Смысл задачи:
Реализовать базовые денежные сценарии: внутренний перевод, пополнение, вывод и компенсацию.

Ожидаемый результат:
Денежные операции выполняются согласованно, создают проводки, соблюдают ограничения баланса и используют блокировки строк в PostgreSQL.

---

## 8. Заявки

Состояние:
Предстоит.

Смысл задачи:
Реализовать создание, просмотр, одобрение и отклонение заявок.

Ожидаемый результат:
Ручные сценарии проходят через заявку, а одобренные заявки создают результат предметной области.

---

## 9. Справочники и режимы работы

Состояние:
Предстоит.

Смысл задачи:
Реализовать чтение и управление справочниками, а также применение режимов работы системы.

Ожидаемый результат:
Поведение регистрации, открытия счёта, перевода, пополнения и вывода управляется режимами VBank без перезапуска приложения.

---

## 10. Журнал аудита

Состояние:
Предстоит.

Смысл задачи:
Реализовать фиксацию значимых действий и результатов.

Ожидаемый результат:
Ключевые действия пользователей, операторов, администраторов и системы сохраняются в журнале аудита и связываются с `request_id`.

---

## 11. Проверки и тесты серверной части

Состояние:
Предстоит.

Смысл задачи:
Проверить, что серверное ядро выполняет основные требования и не нарушает ключевые инварианты.

Ожидаемый результат:
Первая рабочая версия серверной части готова к проверке через внешний консольный клиент.
